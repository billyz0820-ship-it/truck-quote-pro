# 快递订单列表测试指南

## 功能概述
新的快递订单列表页面已对接后端 `/api/v1/Order/GetList` 接口，支持条件查询、分页、排序等功能。

## 核心特性

### 1. 懒加载机制
- **默认不请求数据**：页面初次加载时不自动调用接口
- **按需加载**：用户设置筛选条件并点击"查询"按钮时才请求数据
- **状态切换**：切换状态标签页时自动重新查询

### 2. 高级筛选功能
支持以下筛选条件：
- **基础筛选**：订单号、快递单号、收件人、客户ID
- **承运商筛选**：UPS、FedEx、USPS、DHL等
- **订单类型**：常规订单、一口价订单
- **时间范围**：订单创建时间范围
- **状态筛选**：通过标签页切换订单状态

### 3. 分页与排序
- **分页大小**：支持10/20/50/100条每页
- **排序字段**：订单号、创建时间
- **排序方向**：正序/倒序

### 4. 批量操作
- **批量选择**：支持全选/单选
- **批量打单**：选中多个订单批量打印标签
- **批量导出**：选中多个订单批量导出面单

## API 对接详情

### 请求参数映射
```typescript
{
  pageIndex: number,        // 页码
  pageSize: number,         // 每页数量
  sortField?: string,       // 排序字段
  sortValue?: boolean,      // 排序方向 (true: 正序, false: 倒序)
  orderNo?: string,          // 订单号
  trackingNumber?: string,   // 快递单号
  recipient?: string,        // 收件人
  orderStatus?: OrderImageEnum,  // 订单状态
  customerId?: string,      // 客户ID
  carrier?: LogisticsAccountCarrierEnum,  // 承运商
  startCreateTime?: string, // 订单开始时间
  endCreateTime?: string,   // 订单结束时间
  // ... 其他筛选参数
}
```

### 响应数据结构
```typescript
{
  code: string,
  message: string,
  data: {
    items: GetOrderListResponse[],  // 订单列表
    totalCount: number              // 总数量
  },
  isSuccess: boolean
}
```

## 测试步骤

### 1. 基础功能测试

#### 1.1 页面加载测试
1. 访问 `/dashboard/orders/express` 页面
2. **预期结果**：页面显示"请设置筛选条件并点击查询按钮加载数据"的提示
3. **验证点**：不自动发送API请求

#### 1.2 首次查询测试
1. 不设置任何筛选条件，直接点击"查询"按钮
2. **预期结果**：发送API请求，显示默认分页数据
3. **验证点**：
   - 请求参数：pageIndex=1, pageSize=20
   - 响应数据正确显示在表格中
   - 分页信息正确显示

#### 1.3 高级筛选测试
1. 点击"高级筛选"按钮展开筛选面板
2. 设置筛选条件：
   - 订单号：输入测试订单号
   - 承运商：选择UPS
   - 订单状态：通过标签页选择
   - 时间范围：设置订单创建时间范围
3. 点击"查询"按钮
4. **预期结果**：按照筛选条件查询并显示数据

### 2. 分页功能测试

#### 2.1 页码切换
1. 点击下一页/上一页按钮
2. **验证点**：
   - 页码正确更新
   - API请求参数 pageIndex 正确
   - 数据正确刷新

#### 2.2 页面大小调整
1. 在下拉框中选择不同的页面大小（10/20/50/100）
2. **验证点**：
   - 每页显示数量正确
   - 总页数正确重新计算
   - 重置到第一页

### 3. 排序功能测试

#### 3.1 订单号排序
1. 点击表格头"订单号"列
2. **验证点**：
   - 第一次点击：倒序（默认）
   - 第二次点击：正序
   - API请求参数 sortField="orderNo", sortValue 正确
   - 表格数据按正确顺序显示

#### 3.2 创建时间排序
1. 点击表格头"创建时间"列
2. **验证点**：
   - 排序图标正确显示
   - API请求参数 sortField="createdTime", sortValue 正确
   - 时间排序正确

### 4. 状态标签页测试

#### 4.1 状态切换
1. 点击不同的状态标签页（全部、已创建、待处理等）
2. **验证点**：
   - 标签页正确高亮
   - 自动发送查询请求
   - API请求参数 orderStatus 正确
   - 状态计数正确显示

### 5. 批量操作测试

#### 5.1 批量选择
1. 勾选订单行
2. 点击全选复选框
3. **验证点**：
   - 单选/取消单选正确
   - 全选/取消全选正确
   - 批量操作栏正确显示选中数量

#### 5.2 批量操作按钮
1. 选中多个订单后
2. 点击"批量打单"、"批量导出"按钮
3. **验证点**：
   - 按钮正确显示（仅在有选中项时显示）
   - 按钮文本包含正确的选中数量

### 6. 错误处理测试

#### 6.1 API请求失败
1. 模拟网络错误或服务器错误
2. **验证点**：
   - 显示错误提示信息
   - 不显示加载状态
   - 表格保持之前的状态或显示空数据

#### 6.2 空数据状态
1. 设置不存在的筛选条件
2. **验证点**：
   - 显示"暂无订单数据"
   - 分页控件正确隐藏或显示

## 性能验证

### 1. API请求频率
- **首次加载**：0次请求
- **查询操作**：1次请求
- **状态切换**：1次请求
- **分页操作**：1次请求
- **排序操作**：1次请求

### 2. 数据量测试
- **小数据量**（<100条）：页面响应流畅
- **中等数据量**（100-1000条）：分页正常
- **大数据量**（>1000条）：分页和筛选性能正常

## 调试信息

### 控制台日志
打开浏览器开发者工具，查看控制台日志：
```
=== 开始获取订单列表 ===
当前状态: { activeTab, pagination, filters }
最终查询参数: { API请求参数 }
=== 订单列表响应 ===
订单获取成功: { count, total }
```

### 网络请求监控
在 Network 标签页中：
- 检查请求URL和参数
- 验证响应状态码和数据格式
- 监控请求响应时间

## 常见问题排查

### 问题1：页面空白或加载中状态持续
- 检查API请求是否正常发送
- 检查响应数据格式是否正确
- 查看控制台错误信息

### 问题2：筛选条件不生效
- 检查筛选条件是否正确传递到API
- 验证后端是否正确处理筛选参数
- 查看API请求参数日志

### 问题3：分页显示异常
- 检查totalCount是否正确返回
- 验证pageIndex和pageSize计算逻辑
- 检查分页组件状态更新

### 问题4：排序不正确
- 检查sortField和sortValue参数
- 验证后端排序逻辑
- 确认排序字段名称映射正确

## 预期效果

1. **用户体验**：按需加载，避免不必要的数据请求
2. **性能优化**：合理分页，减少单次数据加载量
3. **功能完整**：支持完整的CRUD操作和批量处理
4. **错误处理**：优雅处理各种异常情况
5. **响应式设计**：适配不同屏幕尺寸